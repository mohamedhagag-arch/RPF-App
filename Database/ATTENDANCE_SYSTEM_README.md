# 📋 Attendance System - Complete Guide

## 🎯 Overview

نظام الحضور والانصراف المتكامل (Attendance Management System) هو نظام شامل لإدارة وتتبع حضور الموظفين مع دعم تتبع الموقع الجغرافي (GPS).

## 📁 Database Setup

### 1. Create Tables

قم بتنفيذ ملف SQL التالي في Supabase SQL Editor:

```sql
-- File: Database/attendance-system-schema.sql
```

هذا الملف سينشئ الجداول التالية:
- `attendance_employees` - بيانات الموظفين
- `attendance_records` - سجلات الحضور والانصراف
- `attendance_locations` - المواقع المسموحة لتسجيل الحضور
- `attendance_settings` - إعدادات النظام

### 2. Verify Tables

تأكد من أن الجداول تم إنشاؤها بنجاح في Supabase Dashboard.

## 🚀 Features

### ✅ Dashboard
- إحصائيات فورية (Total Employees, Present Today, Absent Today, Late Today)
- عرض آخر سجلات الحضور
- إجراءات سريعة للوصول للوظائف الأساسية

### ✅ Employees Management
- إضافة/تعديل/حذف الموظفين
- البحث والفلترة حسب الاسم، الكود، القسم
- عرض إحصائيات الموظفين
- إدارة حالة الموظف (Active/Inactive)

### ✅ Check-In/Check-Out
- تسجيل الحضور والانصراف
- تتبع الموقع الجغرافي (GPS) تلقائياً
- التحقق من أقرب موقع مسموح
- حساب ساعات العمل تلقائياً
- تحديد التأخير والانصراف المبكر
- عرض حالة الحضور اليوم

### ✅ Reports
- تقارير شاملة مع فلاتر متقدمة
- فلترة حسب التاريخ، الموظف، القسم، الحالة
- تصدير التقارير (Excel, CSV)
- إحصائيات مفصلة (Total Records, Check-Ins, Late Count, Average Hours)

## 📖 Usage Guide

### 1. إضافة موظف جديد

1. اذهب إلى تبويب **Employees**
2. اضغط على **Add Employee**
3. املأ البيانات المطلوبة:
   - Employee Code (مطلوب)
   - Name (مطلوب)
   - Job Title (اختياري)
   - Department (اختياري)
   - Phone Number (اختياري)
   - Email (اختياري)
   - Status (Active/Inactive)
4. اضغط **Add Employee**

### 2. تسجيل الحضور

1. اذهب إلى تبويب **Check-In/Out**
2. اختر الموظف من القائمة المنسدلة
3. تأكد من تفعيل الموقع الجغرافي (GPS)
4. اضغط **Check In**
5. النظام سيقوم تلقائياً بـ:
   - تسجيل الوقت والتاريخ
   - حفظ الموقع الجغرافي
   - التحقق من أقرب موقع مسموح
   - تحديد إذا كان الموظف متأخراً

### 3. تسجيل الانصراف

1. في نفس صفحة **Check-In/Out**
2. تأكد من أن الموظف قد سجل الحضور
3. اضغط **Check Out**
4. النظام سيقوم تلقائياً بـ:
   - حساب ساعات العمل
   - تحديد إذا كان الانصراف مبكراً
   - حفظ الموقع الجغرافي

### 4. عرض التقارير

1. اذهب إلى تبويب **Reports**
2. حدد الفلاتر المطلوبة:
   - تاريخ البداية والنهاية
   - الأقسام
   - الحالة (On Time, Late, Early)
3. اضغط **Refresh** لعرض النتائج
4. اضغط **Export** لتصدير التقرير

## ⚙️ Settings

### Work Start Time
الوقت الافتراضي لبدء العمل (08:30)

### Late Allowance
عدد الدقائق المسموحة قبل اعتبار الموظف متأخراً (15 دقيقة)

### Location Required
تفعيل/تعطيل اشتراط الموقع الجغرافي لتسجيل الحضور

### Location Radius
نصف القطر المسموح للموقع (100 متر افتراضي)

## 🔒 Permissions

النظام يستخدم نظام الصلاحيات الموجود في التطبيق:
- **Admin/Manager**: يمكنهم إدارة جميع الموظفين والسجلات
- **Employee**: يمكنهم تسجيل حضورهم فقط
- **Viewer**: يمكنهم عرض التقارير فقط

## 📍 GPS Location Tracking

النظام يدعم تتبع الموقع الجغرافي تلقائياً:
1. عند فتح صفحة Check-In/Out، سيطلب النظام إذن الموقع
2. يتم حفظ الموقع الجغرافي مع كل سجل حضور
3. يتم التحقق من أقرب موقع مسموح تلقائياً
4. يمكن إضافة مواقع جديدة من خلال جدول `attendance_locations`

## 🗄️ Database Tables

### attendance_employees
```sql
- id (UUID)
- employee_code (TEXT, UNIQUE)
- name (TEXT)
- job_title (TEXT)
- department (TEXT)
- phone_number (TEXT)
- email (TEXT, UNIQUE)
- status (Active/Inactive)
- user_id (UUID, FK to auth.users)
```

### attendance_records
```sql
- id (UUID)
- employee_id (UUID, FK)
- check_time (TIME)
- date (DATE)
- type (Check-In/Check-Out)
- location_id (UUID, FK)
- latitude (DECIMAL)
- longitude (DECIMAL)
- notes (TEXT)
- work_duration_hours (DECIMAL)
- is_late (BOOLEAN)
- is_early (BOOLEAN)
```

### attendance_locations
```sql
- id (UUID)
- name (TEXT)
- latitude (DECIMAL)
- longitude (DECIMAL)
- radius_meters (INTEGER)
- description (TEXT)
- is_active (BOOLEAN)
```

## 🔧 Troubleshooting

### الموقع الجغرافي لا يعمل
- تأكد من تفعيل GPS في المتصفح
- تأكد من منح الإذن للموقع
- جرب تحديث الصفحة

### لا يمكن تسجيل الحضور
- تأكد من وجود اتصال بالإنترنت
- تأكد من اختيار موظف
- تأكد من تفعيل الموقع الجغرافي

### التقارير فارغة
- تحقق من الفلاتر المحددة
- تأكد من وجود سجلات في الفترة المحددة

## 📝 Notes

- النظام يحفظ الوقت فقط (HH:mm) وليس التاريخ الكامل
- التاريخ يتم حفظه بشكل منفصل في عمود `date`
- ساعات العمل تُحسب تلقائياً عند تسجيل الانصراف
- النظام يدعم Offline Mode جزئياً (سيتم حفظ البيانات محلياً ثم مزامنتها)

## 🎉 Ready to Use!

النظام جاهز للاستخدام بعد تنفيذ ملف SQL وإنشاء الجداول في Supabase.

---

**تم التطوير بواسطة: AI Assistant**  
**© 2024 جميع الحقوق محفوظة**

